commands:
  01_download_docdb_ca_bundle:
    command: "mkdir -p /tmp && curl -fLsS https://truststore.pki.rds.amazonaws.com/global/global-bundle.pem -o /tmp/global-bundle.pem"
  02_permissions:
    command: "chmod 0644 /tmp/global-bundle.pem"
